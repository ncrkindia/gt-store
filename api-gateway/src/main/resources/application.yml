server:
  port: 4003

spring:
  application:
    name: api-gateway
  config:
    import: "optional:configserver:http://localhost:8888"

# Note: In Docker, the spring.config.import is overridden by environment variable
# SPRING_CONFIG_IMPORT=optional:configserver:http://config-server:8888
